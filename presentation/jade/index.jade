!!! 5
html(lang='en')
  head
    meta(charset='utf-8')
    title ATLAS | Forum analysis
    meta(name='viewport', content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no')
    link(rel='stylesheet', href='css/reveal.min.css')
    link#theme(rel='stylesheet', href='css/theme/beige.css')
    // For syntax highlighting
    link(rel='stylesheet', href='lib/css/zenburn.css')
    // If the query includes 'print-pdf', use the PDF print sheet
    script
      document.write( '<link rel="stylesheet" href="css/print/' + ( window.location.search.match( /print-pdf/gi ) ? 'pdf' : 'paper' ) + '.css" type="text/css" media="print">' );
    //if lt IE 9
      script(src='lib/js/html5shiv.js')
  body
    .reveal
      // Any section element inside of this container is displayed as a slide
      .slides
        section
          h1 Learn.illinois.edu
          h3 How Moodle forums are being used
          p
            small
              | Presentation by
              a(href='http://www.micheleresposito.com') Michele Esposito
        section
          section
            h2 Problem
            p
              | We do not know how students interact with our website
          section
            h2 How to solve it
            p
              | Data mining!
        section
          h2 The idea
          p
            | Understand how forums are used across different classes
            | by looking at data
        section
          section(data-background='#007777')
            h1 The process
            p
              | How I came up with the conclusions
          section
            h2 Get the list of classes
            p
              | Obtain the whole Fall 2012
              | class catalogue
              br
              | from
              a(href='http://courses.illinois.edu/cis/2012/fall/schedule/') courses.illinois.edu
              p
          section
            h2 Obtain the database from Moodle
            p
              | And start digging inside the data
              p
          section
            h2 Cross the data!
            p
              | Merge the two sources in order to get more results
              p
        section(data-background='#007777')
          h2 Data about UIUC courses 
          p
            | Some results after analyzing the list of all courses:
        - var courses = { "allClasses": "All courses", "genEds": "General Education Courses", "moodle": "Moodle classes", "moodleGenEd": "General Education Courses on Moodle", "online": "Online Courses" }
        - each title, tag in courses
          section(data-transition='linear', data-background-transition='slide')
            section
              h2 #{title}
              ul
                li
                  strong(class=tag + 'Sections')
                  |    Sections
                li
                  strong(class=tag + 'Classes')  
                  |   Courses
                li
                  strong(class=tag + 'Dep') 
                  |   Departments
            mixin subtitle
              p(style="margin-top: 50px; font-size: 20px;") #{title}

            section
              h2 Sections per department
              p
                | Average sections per department:
                strong(class=tag + 'SectionsPerDepartments')
              canvas(id=tag + 'SectionsPerDepartments', width='800', height='400')
              +subtitle

            section
              h2 Sections per department
              p
                | Standard deviaton of sections per department:
                strong(class=tag + 'SectionsPerDepartmentsHistogram')
              canvas(id=tag + 'SectionsPerDepartmentsHistogram', width='800', height='400')
              +subtitle

            section
              h2 Courses per department
              p
                | Average courses per department:
                strong(class=tag + 'ClassesPerDepartment')
              canvas(id=tag + 'ClassesPerDepartment', width='800', height='400')
              +subtitle

            section
              h2 Courses per department
              p
                | Standard deviaton of courses per department:
                strong(class=tag + 'ClassesPerDepartmentHistogram')
              canvas(id=tag + 'ClassesPerDepartmentHistogram', width='800', height='400')
              +subtitle
        //
        // start plotting form data
        //
        section
          section
            // FIXME: remove hard coding
            h2 Forums
            ul
              li
                strong.forumCount
                |   forums.
              li
                strong.forumDiscussionCount 83363
                |   discussions.
              li
                strong.forumPostCount 184742
                |    posts.

              li
                strong.forumDiscPerForum 7.28
                |    average discussions per forum.
              li
                strong.forumPostPerDisc 2.21
                |    average posts per discussion.
          section
            h2 Forum types
            canvas#forumTypes(width='800', height='400')

        - var courses = {"moodle": "All classes on Moodle", "moodleGenEd": "General Education Courses on Moodle", "moodleOnline": "Fully Online Courses" }
        - each title, tag in courses
          section(data-transition='linear', data-background-transition='slide')
            section
              h1 #{title}
            mixin subtitle
              p(style="margin-top: 50px; font-size: 20px;") #{title}
            section
              h2 Forums per courses
              p
                | Average forums per course:
                strong(class=tag + 'ForumPerClass')
              canvas(id=tag + 'ForumPerClass', width='800', height='400')
              +subtitle

            section
              h2 Forums per courses
              p
                | Standard deviaton of forums per course:
                strong(class=tag + 'ForumPerClassHistogram')
              canvas(id=tag + 'ForumPerClassHistogram', width='800', height='400')
              +subtitle

            section
              h2 Forum posts per course:
              p
                | Average forums posts per course:
                strong(class=tag + 'ForumPostsPerClass')
              canvas(id=tag + 'ForumPostsPerClass', width='800', height='400')
              +subtitle

            section
              h2 Forums per department
              p
                | Average forums per department:
                strong(class=tag + 'ForumPerDepartment')
              canvas(id=tag + 'ForumPerDepartment', width='800', height='400')
              +subtitle

            section
              h2 Forum posts per department:
              p
                | Average forums posts per department:
                strong(class=tag + 'ForumPostsPerDepartment')
              canvas(id=tag + 'ForumPostsPerDepartment', width='800', height='400')
              +subtitle

            section
              h2 Forums per department
              p
                | Standard deviaton of forums per department:
                strong(class=tag + 'ForumPerDepartmentHistogram')
              canvas(id=tag + 'ForumPerDepartmentHistogram', width='800', height='400')
              +subtitle

    script(src='lib/js/head.min.js')
    script(src='js/reveal.min.js')
    script
      // Full list of configuration options available here:
      // https://github.com/hakimel/reveal.js#configuration
      Reveal.initialize({
        controls: true,
        progress: true,
        history: true,
        center: true,
        theme: Reveal.getQueryHash().theme, // available themes are in /css/theme
        transition: Reveal.getQueryHash().transition || 'default', // default/cube/page/concave/zoom/linear/fade/none
      // Optional libraries used to extend on reveal.js
        dependencies: [
          { src: 'lib/js/classList.js', condition: function() { return !document.body.classList; } },
          { src: 'plugin/markdown/marked.js', condition: function() { return !!document.querySelector( '[data-markdown]' ); } },
          { src: 'plugin/markdown/markdown.js', condition: function() { return !!document.querySelector( '[data-markdown]' ); } },
          { src: 'plugin/highlight/highlight.js', async: true, callback: function() { hljs.initHighlightingOnLoad(); } },
          { src: 'plugin/zoom-js/zoom.js', async: true, condition: function() { return !!document.body.classList; } },
          { src: 'plugin/notes/notes.js', async: true, condition: function() { return !!document.body.classList; } }
        ]
      });
    script(src='lib/js/jquery-2.0.3.min.js')
    script(src='lib/js/underscore-min.js')
    script(src='lib/js/Chart.min.js')
    script(src='lib/js/d3.min.js')
    script(src='js/Stats.js')
    script(src='js/graph.js')

